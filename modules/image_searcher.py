import selenium

def reverse_image(image):
    # idk if selenium will return json
    json = {}
    parsed_json = json_parser(json)
    return parsed_json

def image_search(query):
    image = []
    return image

def json_parser(json):
    # idk how will I parse the json yet
    # for now akon idea list of links
    parsed_json = []
    return parsed_json
